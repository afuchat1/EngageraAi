kind = "web"
previewPath = "/"
title = "Engagera — AI Platform"
version = "1.0.0"
id = "artifacts/engagera"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24935

[services.development]
run = "pnpm --filter @workspace/engagera run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/engagera", "run", "build" ]
publicDir = "artifacts/engagera/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24935"
BASE_PATH = "/"
